import Link from "next/link";
import { redirect } from "next/navigation";
import { requireSchoolAccess } from "@/server/auth/authorization";
import { listVisibleBranches } from "@/server/school-operations/operations";
import SummerClient from "./summer-client";
export default async function SummerPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;try{await requireSchoolAccess(slug)}catch{redirect(`/login?school=${encodeURIComponent(slug)}`)}const visible=await listVisibleBranches(slug);return <main className="casa-shell min-h-screen bg-[#f2f2ef] text-[#0b0b0a]"><header className="border-b border-black bg-white px-5 py-6 sm:px-8"><div className="mx-auto flex max-w-[1500px] flex-wrap items-end justify-between gap-5"><div><p className="casa-kicker text-black/45">CASA / Summer Programme</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.05em]">Summer attendance</h1><p className="mt-2 max-w-2xl text-sm text-black/50">Run a date-bounded Summer programme without a terminal. Teachers and authorized campus staff mark Present, Late or Absent directly; guardian notifications continue for enrolled CASA students.</p></div><nav className="flex gap-3"><Link className="casa-button" href={`/schools/${encodeURIComponent(slug)}/attendance`}>Attendance</Link><Link className="casa-button" href={`/schools/${encodeURIComponent(slug)}/registry`}>Registry</Link></nav></div></header><SummerClient slug={slug} branches={(visible.branches as Array<{id:string;name:string;is_headquarters?:boolean}>).map(b=>({id:b.id,name:b.name,isHeadquarters:Boolean((b as {is_headquarters?:boolean}).is_headquarters)}))}/></main>}
